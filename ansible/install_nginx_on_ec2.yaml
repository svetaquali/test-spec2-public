---
- name: Configure EC2 Instance
  hosts: all
  gather_facts: false
  connection: ssh
  tasks:
    - name: Install Nginx
      yum:
        name: nginx
        state: present

    - name: Start Nginx service
      service:
        name: nginx
        state: started
        enabled: yes
