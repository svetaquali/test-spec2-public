spec_version: 2-preview
description: ansible hello world

inputs:
  agent:
    type: agent

grains:
  grain_1:
    kind: ansible
    spec: 
      source:
        path: ansible/hello_world.yaml
      agent:
        name: '{{ .inputs.agent }}'


