spec_version: 2-preview

inputs:
  compute_service_name:
    type: string
    default: "eks"

grains:
  helloTF:
    kind: terraform
    spec:
      source:
        path: terraform/1/simple-tf-module
        store: test-spec2-public
      host:
        name: '{{.inputs.compute_service_name}}'
