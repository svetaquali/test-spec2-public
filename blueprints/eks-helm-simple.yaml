spec_version: 2-preview

description: description text

grains:

  helloHelm:
    kind: helm
    spec:
      source:
        path: https://github.com/bitnami/charts/tree/master/bitnami/node
      host:
        cloud-account: aws
        compute-service: eks

      commands:
        - dep up bitnami/node
