kind: bluperint 
spec_version: 2-preview
description: sleep

grains:
  sleep:
    kind: shell
    spec:
      agent:
        name: docker-agent
      activities:
        deploy:
          commands:
            - "sleep 10"