spec_version: 2-preview

inputs:
  compute_service_name:
    type: string
    default: "dev"

grains:
  helloTF:
    kind: terraform
    spec:
      source:
        path: terraform/simple-tf-module
        store: autogen_repo_sandbox_3e8b5800
      host:
        name: '{{.inputs.compute_service_name}}'
